import React, { Component } from 'react';
import { PageGroup } from '../configEditor';

interface Props {
  pageGroup:PageGroup;
}

class Settings extends Component<Props> {

  render() {
    return (
      <h1>
        
      </h1>
    );
  }
}

export default Settings;
